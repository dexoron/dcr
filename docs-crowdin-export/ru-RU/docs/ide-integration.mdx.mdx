---
sidebar_label: Интеграция с IDE
---

# Интеграция с IDE

## VS Code

```bash
dcr gen vscode
```

Генерирует в `.vscode/`:

| Файл              | Назначение                                  |
| ----------------- | ------------------------------------------- |
| `tasks.json`      | Задача `build` (dcr build)                  |
| `launch.json`     | Debug-запуск бинарника                      |
| `settings.json`   | clangd/IntelliSense: include пути, стандарт |
| `extensions.json` | Рекомендует clangd, C/C++                   |

## CLion

```bash
dcr gen clion
```

Генерирует в `.idea/`:

| Файл                                      | Назначение                                       |
| ----------------------------------------- | ------------------------------------------------ |
| `externalTools.xml`                       | Build, Run, Clean, Test как внешние инструменты  |
| `customTargets.xml`                       | Кастомные цели сборки                            |
| `misc.xml`                                | Настройки C/C++ проекта                          |
| `runConfigurations/dcr_build_and_run.xml` | Конфигурации запуска для каждого бинарного файла |

## compile_commands.json

```bash
dcr gen команды компиляции
```

Генерирует `compile_commands.json` в корне проекта. Стандартный формат для:

## информация о проекте

```bash
dcr gen info-проекта
```

Выводит JSON с метаданными проекта (см. [gen-commands](/docs/commands/gen-commands)).
